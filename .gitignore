node_modules
dist
.claude
prev_session_msgs.txt
.env.local
.env

.vercel

# Backups locales generados por scripts de edición masiva
*.bak

# Backups locales del repo / working tree (no se publican)
scripts/backups/
